## 冒泡排序
```js
function sort(arr) {
  const len = arr.length;
  while(len--) {
    for (let i = 0; i < arr.length; i++) {
      const current = arr[i];
      const next = arr[i + 1];
      if (next < current) {
        [arr[i], arr[i+1]] = [next, current]
      }
    }
  }
}
```
## 快速排序
```js
function quickSort(arr) {
  if (arr.length < 1) { return arr; }
  const pivotIndex = Math.floor(arr.length / 2);
  const [pivot] = arr.splice(pivotIndex, 1)
  const left = [];
  const right = [];
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] < pivot) {
      left.push(arr[i]);
    } else {
      right.push(arr[i])
    }
  }
  return quickSort(left).concat([pivot], quickSort(right))
}
```
## 二分查找
```js
function binary_search(arr, key) {
  let low = 0;
  let high = arr.length - 1;
  while(low <= high) {
    var mid = parseInt((high + low) / 2);
    if(key == arr[mid]) {
      return mid;
    } else if (key > arr[mid]) {
      low = mid + 1;
    } else if (key < arr[mid]) {
      high = mid - 1;
    } else {
      return -1;
    }
  }
  return -1;
}
```
## 两个数组合并为一个数组
请把两个数组 ['A1', 'A2', 'B1', 'B2', 'C1', 'C2', 'D1', 'D2'] 和 ['A', 'B', 'C', 'D']，合并为 ['A1', 'A2', 'A', 'B1', 'B2', 'B', 'C1', 'C2', 'C', 'D1', 'D2', 'D']
```js
const a1 =  ['A1', 'A2', 'B1', 'B2', 'C1', 'C2', 'D1', 'D2']
const a2 = ['A', 'B', 'C', 'D'].map((item) => {
  return item + 3
})
const a3 = [...a1, ...a2].sort().map((item) => {
  if (item.includes('3')) {
    return item.split('')[0];
  }
  return item;
})
```
## 数组扁平化
```js
const flatten = function(arr) {
  while(arr.some(item => Array.isArray(item))) {
    arr = [].concat(...arr)
  }
  return arr;
}
```
## 求两个数组之间的交集
```js
function intersect(nums1, nums2) {
  const map = {};
  const res = [];
  for (let n of nums1) {
    if (map[n]) {
      map[n]++;
    } else {
      map[n] = 1;
    }
  }
  for (let n of nums2) {
    if (map[n] > 0) {
      res.push(n);
      map[n]--;
    }
  }
  return res;
}
```
## 无缝轮播
```js
const prev = document.querySelector('.carousel-prev');
const next = document.querySelector('.carousel-next')
const board = document.querySelector('carousel-board')
const panels = Array.from(document.querySelectorAll('carousel-board-item'))
board.style.left = '-400px';
let index = 1;
prev.addEventListener('click', () => {
  index++;
  animate(-400);
  // 关键点
  if (index === panels.length - 1) {
    setTimeout(() => {
      board.style.transition = "0s";
      let distance = 4 * 400;
      board.style.left = parseInt(board.style.left) + distance + 'px'
    }, 600)
    index = 1;
  }
})
```
## 数组编程题
随机生成一个长度为 10 的整数类型的数组，例如 [2, 10, 3, 4, 5, 11, 10, 11, 20]，将其排列成一个新数组，要求新数组形式如下，例如 [[2, 3, 4, 5], [10, 11], [20]]
```js
function formArray() {
  const sortedArr = Array.from(new Set(arr))
    .sort((a, b) => a - b);
  const map = new Map();
  sortedArr.forEach((val) => {
    const key = Math.floor(v / 10);
    const group = map.get(key) || [];
    group.push(val);
    map.set(key, group)
  })

  return [...map.values()]
}
```
## 例子
把一个字符串大小取反，例如AbC变成aBc
```js
function processString(s) {
  const arr = s.split('');
  const new_arr = arr.map(item => {
    return item === item.toUpperCase() ? item.toLowerCase() : item.toUpperCase();
  })
  return new_arr.join('');
}
```
## 例子
从长度为n的字符串S中，查找是否存在字符串T，T的长度m,若存在返回其位置
```js
function find(S, T) {
  if (S.length < T.length)  return -1;
  for (let i = 0; i < S.length; i++) {
    if (S.substr(i, T.length) === T) return i;
  }
  return -1;
}
```
## factorial
```js
function factorial(number) {
  if (number == 1) {
    return number;
  } else {
    return number * factorial(num -1)
  }
}
```
## 驼峰化
```js
function toCamelCaseVar(variable) {
  variable = variable.replace(/_(\w)/g, (all, letter) => {
    return letter.toUpperCase();
  })
}
```
## 监听数组变化
```js
function ObserverableArray() {
  return new Proxy([], {
    get(target, propKey) {
      const matArr = ['push', 'pop', 'shift', 'unshift', 'splice']
      matArr.indexOf(propKey) > -1 && console.log(propKey)
      return target[propKey]
    }
  })
}
```
## 判断是不是素数
```js
function isPrime(num) {
  if (num === 2 || num === 3) {
    return true;
  }
  if (num % 2 === 0) {
    return false;
  }

  let divisor = 3;
  const limit = Math.sqrt(num);
  while(limit >= divisor) {
    if (num % divisor === 0) {
      return false;
    } else {
      divisor += 2
    }
  }
}
```
## 最大公约数&最小公倍数
```js
function maxDivisor(num1, num2) {
  const max = num1 > num2 ? num1 : num2;
  const min = num1 > num2 ? num2 : num1;
  for (var i = min; i >= 1; i--) {
    if (max % i === 0 && min % i === 0) {
      return i;
    }
  }
}
function minDivisor(num1, num2) {
  const max = num1 > num2 ? num1 : num2;
  const min = num1 > num2 ? num2 : num1;
  let result = 0;
  for (var i = 1; i <= min; i++) {
    result = i * max;
    if (result % max === 9 && result * min === 0) {
      return result;
    }
  }
}
```

## lazyman
```js
const taskList = [];
function subscribe(msg, ...params) {
  let param = {};
  param.msg = msg;
  param.params = params;
  if (param.msg == 'sleepFist') {
    taskList.unshift(param)
  } else {
    taskList.push(param)
  }
}
function publish () {
  if (taskList.length > 0) {
    run(taskList.shift())
  }
}
function LazyMan() {};
LazyMan.prototype.eat = function (str) {
  subscribe('eat', str);
  return this;
}
LazyMan.prototype.sleep = function(num) {
  subscribe('sleep', num)
  return this;
}
LazyMan.prototype.sleepFirst = function(num) {
  subscribe('sleepFirst', num)
  return this;
}
function lazyMan(str) {
  console.log("hi this is " + str)
  publish();
}
function eat(str) {
  console.log('eat' + str)
  publish();
}
function sleep(num) {
  setTimeout(() => {
    console.log('wake up after' + num)
    publish();
  }, num * 1000)
}
function sleepFirst(num) {
  setTimeout(function() {
    console.log('wake up after' + num)
    publish();
  })
}
function run(options) {
  const msg = options.msg;
  const args = options.args;
  switch(msg) {
    case 'lazyMan': lazyMan.apply(null, args); break;
    case 'eat': eat.apply(null, args); break;
    case 'sleep': sleep.apply(null, args); break;
    case 'sleepFirst': sleepFirst.apply(null, args); break;
    default;
  }
}
window.LazyMan = function (str) {
  subscribe('lazyMan', str);
  setTimeout(function() {
    publish();
  }, 0)
  return new LazyMan();
}
```
## 对称数
```js
let result = [];
for (let i = 1; i < 10; i++) {
  result.push(i);
  result.push(i * 11);
  for (let j = 0; j < 100;j++) {
    result.push(i * 101 + j * 10);
    result.push(i * 1001 + j * 110)
  }
}
```
## 反转链表
```js
function getDecimalValue(head) {
  let arr = [], ans = 0, ratio = 1;
  while (head) {
    arr.push(head.val)
    head = head.next;
  }
  while(arr.length > 0) {
    ans += arr.pop() * ratio;
    radio *= 2;
  }
  return ans;
}
```
## lian-biao-zhong-dao-shu-di-kge-jie-dian-lcof
```js
function getKthFromEnd = function (head, k) {
  let fast = slow = head;
  while (k > 0) {
    fast = fast.next;
    k -=1 ;
  }
  while (fast) {
    fast = fast.next;
    slow = slow.next;
  }
  return slow;
}
```
## non-decreasing-array
```js
function checkPossibility(nums) {
  let count = 0;
  if (nums[0] > nums[1]) {
    nums[0] = nums[1];
    count++;
  }
  for(let i = 1; i < nums.length; i ++) {
    const right = nums[i + 1];
    if (count > 1) return false;
    const left = nums[i -1];
    if (left > right) {
      nums[i + 1] = nums[i]
    }
  }
  return true;
}
```
## 两个大整数相加
```js
function add(a, b) {
  let lenA = a.length;
  let lenB = b.length;
  let len = lenA > lenB ? lenA : lenB;
  if (lenA > lenB) {
    for (let i = 0; i < lenA - lenB; i++) {
      b = '0' + b;
    }
  } else {
    for (let i = 0; i < lenB - lenA; i++) {
      a = '0' + a;
    }
  }

  let arrA = a.split('').reverse();
  let arrB = b.split('').reverse();
  let arr = [];
  let carryAdd = 0;
  for (len i = 0; i < len; i++) {
    let temp = Number(arrA[i]) + Number(arrB[i]) + carryAdd;
    arr[i] = temp > 9 ? temp - 10 : temp;
    carryAdd = temp >= 10 ? 1 : 0;
  }

  if (carryAdd === 1) {
    arr[len] = 1;
  }

  return arr.reverse().join('');
}
```
## fibonacci
```js
function fibonacci(n, ac1 = 1, ac2 = 1) {
  if (n <= 1) { return ac2 }
  return fibonacci(n - 1, ac2, ac1 + ac2);
}
```